# ShopperMatch.AI backend dependencies (pinned to known-good releases)
fastapi==0.115.6
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
asyncpg==0.30.0
aiosqlite==0.20.0
pydantic==2.10.4
pydantic-settings==2.7.0
alembic==1.14.0
greenlet==3.1.1
python-multipart==0.0.19
email-validator==2.2.0
dnspython==2.8.0
cryptography==50.0.0
reportlab==4.2.5
openpyxl==3.1.5

# Social Media post composer: extracting text from a client-uploaded
# reference document (.pdf/.docx) for AI post generation (services/social_ai.py).
pypdf==5.1.0
python-docx==1.1.2

# Test / verification tooling
httpx==0.28.1
pytest==8.3.4
pytest-asyncio==0.25.0

# zoneinfo (stdlib) has no bundled tzdata on Windows — needed for Social
# Media Automation Rules' schedule_time-in-a-timezone computation
# (services/social_automation.py). Harmless no-op on Linux/macOS, which
# normally ship system tzdata already.
tzdata; sys_platform == "win32"
